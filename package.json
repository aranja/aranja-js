{
  "name": "aranja-js",
  "version": "1.0.0",
  "description": "A collection of JS utilities used in various projects.",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d build",
    "flow": "flow; test $? -eq 0 -o $? -eq 2",
    "lint": "eslint src/**/*.js",
    "test": "npm run flow && npm run lint"
  },
  "author": "Aranja",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-eslint": "^7.0.0",
    "babel-preset-flow": "^1.0.0",
    "babel-preset-latest": "^6.16.0",
    "eslint": "^3.8.1",
    "eslint-plugin-flowtype": "^2.21.0",
    "flow-bin": "^0.33.0"
  }
}
